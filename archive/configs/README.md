# Configuration archive

- `legacy_202607/` preserves snapshots of the former active root configs for
  campaign `mobo_target10um_v1` (baseline: 1,209 observations).
- `legacy_named_configs_202607/` preserves the four older scalar,
  lexicographic, and multi-host named configs that were formerly in
  `configs/`.

The only active configs are now the root `bo_config.json` and
`bo_multihost_config.json`. They identify the fresh structured,
finite-radius campaign and use a baseline of zero.
